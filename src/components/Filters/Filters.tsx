import { FilterInput } from "./Filters.styled";

export const Filters = () => {
  return (
    <div>
      <FilterInput type="text" />
      <button></button>
      <button></button>
    </div>
  );
};
